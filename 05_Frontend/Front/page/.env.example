# URL base de la API de MailConnect (AWS API Gateway).
# Copia este archivo como ".env" y ajusta el valor según el entorno.
# Si no se define, el front usa por defecto el stage "Test".
VITE_API_BASE_URL=https://mtgt9qpb77.execute-api.us-east-1.amazonaws.com/Test/api
