# Rutas ADMIN (POST, integración no-proxy) que necesitan el context del Authorizer
# (role/tenant) inyectado por el mapping template. Una ruta por línea.
# Las líneas que empiezan con # y las vacías se ignoran.
#
# Este archivo es la ÚNICA fuente de verdad: agrega/quita una ruta aquí, haz push,
# y el workflow deploy-api.yml aplica el mapping template + CORS y despliega.
#
# El PREFIX (p. ej. /V1) NO va aquí; se pasa como variable al workflow/script.

/Customer/List
/Customer/Update
/Customer/Detail
/User/SetRole
/Pricing/List
/Pricing/Update
/Billing/Summary
/Admin/Dashboard
/Admin/Jobs
/Admin/Audit
/Config/Get
/Config/Set
